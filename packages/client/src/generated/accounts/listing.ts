/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getListingStatusDecoder,
  getListingStatusEncoder,
  type ListingStatus,
  type ListingStatusArgs,
} from '../types';

export const LISTING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  218, 32, 50, 73, 43, 134, 26, 58,
]);

export function getListingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LISTING_DISCRIMINATOR);
}

export type Listing = {
  discriminator: ReadonlyUint8Array;
  ticket: Address;
  event: Address;
  seller: Address;
  listingId: number;
  priceLamports: bigint;
  status: ListingStatus;
  createdAt: bigint;
  expiresAt: Option<bigint>;
  buyer: Option<Address>;
  bump: number;
};

export type ListingArgs = {
  ticket: Address;
  event: Address;
  seller: Address;
  listingId: number;
  priceLamports: number | bigint;
  status: ListingStatusArgs;
  createdAt: number | bigint;
  expiresAt: OptionOrNullable<number | bigint>;
  buyer: OptionOrNullable<Address>;
  bump: number;
};

/** Gets the encoder for {@link ListingArgs} account data. */
export function getListingEncoder(): Encoder<ListingArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['ticket', getAddressEncoder()],
      ['event', getAddressEncoder()],
      ['seller', getAddressEncoder()],
      ['listingId', getU32Encoder()],
      ['priceLamports', getU64Encoder()],
      ['status', getListingStatusEncoder()],
      ['createdAt', getI64Encoder()],
      ['expiresAt', getOptionEncoder(getI64Encoder())],
      ['buyer', getOptionEncoder(getAddressEncoder())],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: LISTING_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Listing} account data. */
export function getListingDecoder(): Decoder<Listing> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['ticket', getAddressDecoder()],
    ['event', getAddressDecoder()],
    ['seller', getAddressDecoder()],
    ['listingId', getU32Decoder()],
    ['priceLamports', getU64Decoder()],
    ['status', getListingStatusDecoder()],
    ['createdAt', getI64Decoder()],
    ['expiresAt', getOptionDecoder(getI64Decoder())],
    ['buyer', getOptionDecoder(getAddressDecoder())],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Listing} account data. */
export function getListingCodec(): Codec<ListingArgs, Listing> {
  return combineCodec(getListingEncoder(), getListingDecoder());
}

export function decodeListing<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Listing, TAddress>;
export function decodeListing<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Listing, TAddress>;
export function decodeListing<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Listing, TAddress> | MaybeAccount<Listing, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getListingDecoder());
}

export async function fetchListing<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Listing, TAddress>> {
  const maybeAccount = await fetchMaybeListing(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeListing<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Listing, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeListing(maybeAccount);
}

export async function fetchAllListing(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Listing>[]> {
  const maybeAccounts = await fetchAllMaybeListing(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeListing(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Listing>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeListing(maybeAccount));
}
