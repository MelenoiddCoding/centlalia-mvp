/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import { findPlatformConfigPda, findTierPda } from '../pdas';
import { CENTLALIA_TICKETING_PROGRAM_ADDRESS } from '../programs';

export const ADD_TIER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  92, 225, 133, 110, 149, 39, 185, 4,
]);

export function getAddTierDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_TIER_DISCRIMINATOR);
}

export type AddTierInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountOrganizer extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountTier extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrganizer extends string
        ? WritableSignerAccount<TAccountOrganizer> & AccountSignerMeta<TAccountOrganizer>
        : TAccountOrganizer,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountEvent extends string ? WritableAccount<TAccountEvent> : TAccountEvent,
      TAccountTier extends string ? WritableAccount<TAccountTier> : TAccountTier,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddTierInstructionData = {
  discriminator: ReadonlyUint8Array;
  tierId: number;
  name: string;
  priceLamports: bigint;
  supply: number;
};

export type AddTierInstructionDataArgs = {
  tierId: number;
  name: string;
  priceLamports: number | bigint;
  supply: number;
};

export function getAddTierInstructionDataEncoder(): Encoder<AddTierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['tierId', getU16Encoder()],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['priceLamports', getU64Encoder()],
      ['supply', getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_TIER_DISCRIMINATOR }),
  );
}

export function getAddTierInstructionDataDecoder(): Decoder<AddTierInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['tierId', getU16Decoder()],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['priceLamports', getU64Decoder()],
    ['supply', getU32Decoder()],
  ]);
}

export function getAddTierInstructionDataCodec(): Codec<
  AddTierInstructionDataArgs,
  AddTierInstructionData
> {
  return combineCodec(getAddTierInstructionDataEncoder(), getAddTierInstructionDataDecoder());
}

export type AddTierAsyncInput<
  TAccountOrganizer extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTier extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  platformConfig?: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  tier?: Address<TAccountTier>;
  systemProgram?: Address<TAccountSystemProgram>;
  tierId: AddTierInstructionDataArgs['tierId'];
  name: AddTierInstructionDataArgs['name'];
  priceLamports: AddTierInstructionDataArgs['priceLamports'];
  supply: AddTierInstructionDataArgs['supply'];
};

export async function getAddTierInstructionAsync<
  TAccountOrganizer extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTier extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: AddTierAsyncInput<
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddTierInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
    tier: { value: input.tier ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }
  if (!accounts.tier.value) {
    accounts.tier.value = await findTierPda({
      event: getAddressFromResolvedInstructionAccount('event', accounts.event.value),
      tierId: getNonNullResolvedInstructionInput('tierId', args.tierId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('organizer', accounts.organizer),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('tier', accounts.tier),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getAddTierInstructionDataEncoder().encode(args as AddTierInstructionDataArgs),
    programAddress,
  } as AddTierInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier,
    TAccountSystemProgram
  >);
}

export type AddTierInput<
  TAccountOrganizer extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTier extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  platformConfig: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  tier: Address<TAccountTier>;
  systemProgram?: Address<TAccountSystemProgram>;
  tierId: AddTierInstructionDataArgs['tierId'];
  name: AddTierInstructionDataArgs['name'];
  priceLamports: AddTierInstructionDataArgs['priceLamports'];
  supply: AddTierInstructionDataArgs['supply'];
};

export function getAddTierInstruction<
  TAccountOrganizer extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTier extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: AddTierInput<
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddTierInstruction<
  TProgramAddress,
  TAccountOrganizer,
  TAccountPlatformConfig,
  TAccountEvent,
  TAccountTier,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
    tier: { value: input.tier ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('organizer', accounts.organizer),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('tier', accounts.tier),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getAddTierInstructionDataEncoder().encode(args as AddTierInstructionDataArgs),
    programAddress,
  } as AddTierInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier,
    TAccountSystemProgram
  >);
}

export type ParsedAddTierInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    organizer: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    event: TAccountMetas[2];
    tier: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: AddTierInstructionData;
};

export function parseAddTierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddTierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      organizer: getNextAccount(),
      platformConfig: getNextAccount(),
      event: getNextAccount(),
      tier: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddTierInstructionDataDecoder().decode(instruction.data),
  };
}
