/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import {
  findAssetAuthorityPda,
  findListingPda,
  findManagedAssetPda,
  findPlatformConfigPda,
} from '../pdas';
import { CENTLALIA_TICKETING_PROGRAM_ADDRESS } from '../programs';

export const LIST_TICKET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  11, 213, 240, 45, 246, 35, 44, 162,
]);

export function getListTicketDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(LIST_TICKET_DISCRIMINATOR);
}

export type ListTicketInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountSeller extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountTicketRecord extends string | AccountMeta<string> = string,
  TAccountManagedAsset extends string | AccountMeta<string> = string,
  TAccountAssetAuthority extends string | AccountMeta<string> = string,
  TAccountListing extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSeller extends string
        ? WritableSignerAccount<TAccountSeller> & AccountSignerMeta<TAccountSeller>
        : TAccountSeller,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountEvent extends string ? ReadonlyAccount<TAccountEvent> : TAccountEvent,
      TAccountTicketRecord extends string
        ? WritableAccount<TAccountTicketRecord>
        : TAccountTicketRecord,
      TAccountManagedAsset extends string
        ? ReadonlyAccount<TAccountManagedAsset>
        : TAccountManagedAsset,
      TAccountAssetAuthority extends string
        ? ReadonlyAccount<TAccountAssetAuthority>
        : TAccountAssetAuthority,
      TAccountListing extends string ? WritableAccount<TAccountListing> : TAccountListing,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ListTicketInstructionData = {
  discriminator: ReadonlyUint8Array;
  listingId: number;
  priceLamports: bigint;
  expiresAt: bigint;
};

export type ListTicketInstructionDataArgs = {
  listingId: number;
  priceLamports: number | bigint;
  expiresAt: number | bigint;
};

export function getListTicketInstructionDataEncoder(): FixedSizeEncoder<ListTicketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['listingId', getU32Encoder()],
      ['priceLamports', getU64Encoder()],
      ['expiresAt', getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: LIST_TICKET_DISCRIMINATOR }),
  );
}

export function getListTicketInstructionDataDecoder(): FixedSizeDecoder<ListTicketInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['listingId', getU32Decoder()],
    ['priceLamports', getU64Decoder()],
    ['expiresAt', getI64Decoder()],
  ]);
}

export function getListTicketInstructionDataCodec(): FixedSizeCodec<
  ListTicketInstructionDataArgs,
  ListTicketInstructionData
> {
  return combineCodec(getListTicketInstructionDataEncoder(), getListTicketInstructionDataDecoder());
}

export type ListTicketAsyncInput<
  TAccountSeller extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTicketRecord extends string = string,
  TAccountManagedAsset extends string = string,
  TAccountAssetAuthority extends string = string,
  TAccountListing extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  seller: TransactionSigner<TAccountSeller>;
  platformConfig?: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  ticketRecord: Address<TAccountTicketRecord>;
  managedAsset?: Address<TAccountManagedAsset>;
  assetAuthority?: Address<TAccountAssetAuthority>;
  listing?: Address<TAccountListing>;
  systemProgram?: Address<TAccountSystemProgram>;
  listingId: ListTicketInstructionDataArgs['listingId'];
  priceLamports: ListTicketInstructionDataArgs['priceLamports'];
  expiresAt: ListTicketInstructionDataArgs['expiresAt'];
};

export async function getListTicketInstructionAsync<
  TAccountSeller extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTicketRecord extends string,
  TAccountManagedAsset extends string,
  TAccountAssetAuthority extends string,
  TAccountListing extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: ListTicketAsyncInput<
    TAccountSeller,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountListing,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ListTicketInstruction<
    TProgramAddress,
    TAccountSeller,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountListing,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    seller: { value: input.seller ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: false },
    ticketRecord: { value: input.ticketRecord ?? null, isWritable: true },
    managedAsset: { value: input.managedAsset ?? null, isWritable: false },
    assetAuthority: { value: input.assetAuthority ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }
  if (!accounts.managedAsset.value) {
    accounts.managedAsset.value = await findManagedAssetPda({
      ticketRecord: getAddressFromResolvedInstructionAccount(
        'ticketRecord',
        accounts.ticketRecord.value,
      ),
    });
  }
  if (!accounts.assetAuthority.value) {
    accounts.assetAuthority.value = await findAssetAuthorityPda({
      platformConfig: getAddressFromResolvedInstructionAccount(
        'platformConfig',
        accounts.platformConfig.value,
      ),
    });
  }
  if (!accounts.listing.value) {
    accounts.listing.value = await findListingPda({
      ticketRecord: getAddressFromResolvedInstructionAccount(
        'ticketRecord',
        accounts.ticketRecord.value,
      ),
      listingId: getNonNullResolvedInstructionInput('listingId', args.listingId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('seller', accounts.seller),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('ticketRecord', accounts.ticketRecord),
      getAccountMeta('managedAsset', accounts.managedAsset),
      getAccountMeta('assetAuthority', accounts.assetAuthority),
      getAccountMeta('listing', accounts.listing),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getListTicketInstructionDataEncoder().encode(args as ListTicketInstructionDataArgs),
    programAddress,
  } as ListTicketInstruction<
    TProgramAddress,
    TAccountSeller,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountListing,
    TAccountSystemProgram
  >);
}

export type ListTicketInput<
  TAccountSeller extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTicketRecord extends string = string,
  TAccountManagedAsset extends string = string,
  TAccountAssetAuthority extends string = string,
  TAccountListing extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  seller: TransactionSigner<TAccountSeller>;
  platformConfig: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  ticketRecord: Address<TAccountTicketRecord>;
  managedAsset: Address<TAccountManagedAsset>;
  assetAuthority: Address<TAccountAssetAuthority>;
  listing: Address<TAccountListing>;
  systemProgram?: Address<TAccountSystemProgram>;
  listingId: ListTicketInstructionDataArgs['listingId'];
  priceLamports: ListTicketInstructionDataArgs['priceLamports'];
  expiresAt: ListTicketInstructionDataArgs['expiresAt'];
};

export function getListTicketInstruction<
  TAccountSeller extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTicketRecord extends string,
  TAccountManagedAsset extends string,
  TAccountAssetAuthority extends string,
  TAccountListing extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: ListTicketInput<
    TAccountSeller,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountListing,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ListTicketInstruction<
  TProgramAddress,
  TAccountSeller,
  TAccountPlatformConfig,
  TAccountEvent,
  TAccountTicketRecord,
  TAccountManagedAsset,
  TAccountAssetAuthority,
  TAccountListing,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    seller: { value: input.seller ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: false },
    ticketRecord: { value: input.ticketRecord ?? null, isWritable: true },
    managedAsset: { value: input.managedAsset ?? null, isWritable: false },
    assetAuthority: { value: input.assetAuthority ?? null, isWritable: false },
    listing: { value: input.listing ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('seller', accounts.seller),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('ticketRecord', accounts.ticketRecord),
      getAccountMeta('managedAsset', accounts.managedAsset),
      getAccountMeta('assetAuthority', accounts.assetAuthority),
      getAccountMeta('listing', accounts.listing),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getListTicketInstructionDataEncoder().encode(args as ListTicketInstructionDataArgs),
    programAddress,
  } as ListTicketInstruction<
    TProgramAddress,
    TAccountSeller,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountListing,
    TAccountSystemProgram
  >);
}

export type ParsedListTicketInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    seller: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    event: TAccountMetas[2];
    ticketRecord: TAccountMetas[3];
    managedAsset: TAccountMetas[4];
    assetAuthority: TAccountMetas[5];
    listing: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: ListTicketInstructionData;
};

export function parseListTicketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedListTicketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 8,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      seller: getNextAccount(),
      platformConfig: getNextAccount(),
      event: getNextAccount(),
      ticketRecord: getNextAccount(),
      managedAsset: getNextAccount(),
      assetAuthority: getNextAccount(),
      listing: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getListTicketInstructionDataDecoder().decode(instruction.data),
  };
}
