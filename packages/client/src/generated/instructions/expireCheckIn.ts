/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import { CENTLALIA_TICKETING_PROGRAM_ADDRESS } from '../programs';

export const EXPIRE_CHECK_IN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  65, 135, 242, 109, 30, 73, 85, 6,
]);

export function getExpireCheckInDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXPIRE_CHECK_IN_DISCRIMINATOR);
}

export type ExpireCheckInInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TAccountTicketRecord extends string | AccountMeta<string> = string,
  TAccountCheckInIntent extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> & AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      TAccountTicketRecord extends string
        ? WritableAccount<TAccountTicketRecord>
        : TAccountTicketRecord,
      TAccountCheckInIntent extends string
        ? WritableAccount<TAccountCheckInIntent>
        : TAccountCheckInIntent,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireCheckInInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireCheckInInstructionDataArgs = {};

export function getExpireCheckInInstructionDataEncoder(): FixedSizeEncoder<ExpireCheckInInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXPIRE_CHECK_IN_DISCRIMINATOR }),
  );
}

export function getExpireCheckInInstructionDataDecoder(): FixedSizeDecoder<ExpireCheckInInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getExpireCheckInInstructionDataCodec(): FixedSizeCodec<
  ExpireCheckInInstructionDataArgs,
  ExpireCheckInInstructionData
> {
  return combineCodec(
    getExpireCheckInInstructionDataEncoder(),
    getExpireCheckInInstructionDataDecoder(),
  );
}

export type ExpireCheckInInput<
  TAccountKeeper extends string = string,
  TAccountTicketRecord extends string = string,
  TAccountCheckInIntent extends string = string,
> = {
  keeper: TransactionSigner<TAccountKeeper>;
  ticketRecord: Address<TAccountTicketRecord>;
  checkInIntent: Address<TAccountCheckInIntent>;
};

export function getExpireCheckInInstruction<
  TAccountKeeper extends string,
  TAccountTicketRecord extends string,
  TAccountCheckInIntent extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: ExpireCheckInInput<TAccountKeeper, TAccountTicketRecord, TAccountCheckInIntent>,
  config?: { programAddress?: TProgramAddress },
): ExpireCheckInInstruction<
  TProgramAddress,
  TAccountKeeper,
  TAccountTicketRecord,
  TAccountCheckInIntent
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: false },
    ticketRecord: { value: input.ticketRecord ?? null, isWritable: true },
    checkInIntent: { value: input.checkInIntent ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('keeper', accounts.keeper),
      getAccountMeta('ticketRecord', accounts.ticketRecord),
      getAccountMeta('checkInIntent', accounts.checkInIntent),
    ],
    data: getExpireCheckInInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireCheckInInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountTicketRecord,
    TAccountCheckInIntent
  >);
}

export type ParsedExpireCheckInInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    keeper: TAccountMetas[0];
    ticketRecord: TAccountMetas[1];
    checkInIntent: TAccountMetas[2];
  };
  data: ExpireCheckInInstructionData;
};

export function parseExpireCheckInInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpireCheckInInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      keeper: getNextAccount(),
      ticketRecord: getNextAccount(),
      checkInIntent: getNextAccount(),
    },
    data: getExpireCheckInInstructionDataDecoder().decode(instruction.data),
  };
}
