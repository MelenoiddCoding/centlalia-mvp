/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import { findAssetAuthorityPda, findManagedAssetPda, findPlatformConfigPda } from '../pdas';
import { CENTLALIA_TICKETING_PROGRAM_ADDRESS } from '../programs';

export const GIFT_TICKET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  150, 80, 29, 171, 14, 135, 247, 129,
]);

export function getGiftTicketDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GIFT_TICKET_DISCRIMINATOR);
}

export type GiftTicketInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountCurrentOwner extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountTicketRecord extends string | AccountMeta<string> = string,
  TAccountManagedAsset extends string | AccountMeta<string> = string,
  TAccountAssetAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCurrentOwner extends string
        ? ReadonlySignerAccount<TAccountCurrentOwner> & AccountSignerMeta<TAccountCurrentOwner>
        : TAccountCurrentOwner,
      TAccountRecipient extends string ? ReadonlyAccount<TAccountRecipient> : TAccountRecipient,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountEvent extends string ? ReadonlyAccount<TAccountEvent> : TAccountEvent,
      TAccountTicketRecord extends string
        ? WritableAccount<TAccountTicketRecord>
        : TAccountTicketRecord,
      TAccountManagedAsset extends string
        ? WritableAccount<TAccountManagedAsset>
        : TAccountManagedAsset,
      TAccountAssetAuthority extends string
        ? ReadonlyAccount<TAccountAssetAuthority>
        : TAccountAssetAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type GiftTicketInstructionData = { discriminator: ReadonlyUint8Array };

export type GiftTicketInstructionDataArgs = {};

export function getGiftTicketInstructionDataEncoder(): FixedSizeEncoder<GiftTicketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GIFT_TICKET_DISCRIMINATOR }),
  );
}

export function getGiftTicketInstructionDataDecoder(): FixedSizeDecoder<GiftTicketInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getGiftTicketInstructionDataCodec(): FixedSizeCodec<
  GiftTicketInstructionDataArgs,
  GiftTicketInstructionData
> {
  return combineCodec(getGiftTicketInstructionDataEncoder(), getGiftTicketInstructionDataDecoder());
}

export type GiftTicketAsyncInput<
  TAccountCurrentOwner extends string = string,
  TAccountRecipient extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTicketRecord extends string = string,
  TAccountManagedAsset extends string = string,
  TAccountAssetAuthority extends string = string,
> = {
  currentOwner: TransactionSigner<TAccountCurrentOwner>;
  recipient: Address<TAccountRecipient>;
  platformConfig?: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  ticketRecord: Address<TAccountTicketRecord>;
  managedAsset?: Address<TAccountManagedAsset>;
  assetAuthority?: Address<TAccountAssetAuthority>;
};

export async function getGiftTicketInstructionAsync<
  TAccountCurrentOwner extends string,
  TAccountRecipient extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTicketRecord extends string,
  TAccountManagedAsset extends string,
  TAccountAssetAuthority extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: GiftTicketAsyncInput<
    TAccountCurrentOwner,
    TAccountRecipient,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  GiftTicketInstruction<
    TProgramAddress,
    TAccountCurrentOwner,
    TAccountRecipient,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    currentOwner: { value: input.currentOwner ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: false },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: false },
    ticketRecord: { value: input.ticketRecord ?? null, isWritable: true },
    managedAsset: { value: input.managedAsset ?? null, isWritable: true },
    assetAuthority: { value: input.assetAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }
  if (!accounts.managedAsset.value) {
    accounts.managedAsset.value = await findManagedAssetPda({
      ticketRecord: getAddressFromResolvedInstructionAccount(
        'ticketRecord',
        accounts.ticketRecord.value,
      ),
    });
  }
  if (!accounts.assetAuthority.value) {
    accounts.assetAuthority.value = await findAssetAuthorityPda({
      platformConfig: getAddressFromResolvedInstructionAccount(
        'platformConfig',
        accounts.platformConfig.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('currentOwner', accounts.currentOwner),
      getAccountMeta('recipient', accounts.recipient),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('ticketRecord', accounts.ticketRecord),
      getAccountMeta('managedAsset', accounts.managedAsset),
      getAccountMeta('assetAuthority', accounts.assetAuthority),
    ],
    data: getGiftTicketInstructionDataEncoder().encode({}),
    programAddress,
  } as GiftTicketInstruction<
    TProgramAddress,
    TAccountCurrentOwner,
    TAccountRecipient,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority
  >);
}

export type GiftTicketInput<
  TAccountCurrentOwner extends string = string,
  TAccountRecipient extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTicketRecord extends string = string,
  TAccountManagedAsset extends string = string,
  TAccountAssetAuthority extends string = string,
> = {
  currentOwner: TransactionSigner<TAccountCurrentOwner>;
  recipient: Address<TAccountRecipient>;
  platformConfig: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  ticketRecord: Address<TAccountTicketRecord>;
  managedAsset: Address<TAccountManagedAsset>;
  assetAuthority: Address<TAccountAssetAuthority>;
};

export function getGiftTicketInstruction<
  TAccountCurrentOwner extends string,
  TAccountRecipient extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTicketRecord extends string,
  TAccountManagedAsset extends string,
  TAccountAssetAuthority extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: GiftTicketInput<
    TAccountCurrentOwner,
    TAccountRecipient,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): GiftTicketInstruction<
  TProgramAddress,
  TAccountCurrentOwner,
  TAccountRecipient,
  TAccountPlatformConfig,
  TAccountEvent,
  TAccountTicketRecord,
  TAccountManagedAsset,
  TAccountAssetAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    currentOwner: { value: input.currentOwner ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: false },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: false },
    ticketRecord: { value: input.ticketRecord ?? null, isWritable: true },
    managedAsset: { value: input.managedAsset ?? null, isWritable: true },
    assetAuthority: { value: input.assetAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('currentOwner', accounts.currentOwner),
      getAccountMeta('recipient', accounts.recipient),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('ticketRecord', accounts.ticketRecord),
      getAccountMeta('managedAsset', accounts.managedAsset),
      getAccountMeta('assetAuthority', accounts.assetAuthority),
    ],
    data: getGiftTicketInstructionDataEncoder().encode({}),
    programAddress,
  } as GiftTicketInstruction<
    TProgramAddress,
    TAccountCurrentOwner,
    TAccountRecipient,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority
  >);
}

export type ParsedGiftTicketInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    currentOwner: TAccountMetas[0];
    recipient: TAccountMetas[1];
    platformConfig: TAccountMetas[2];
    event: TAccountMetas[3];
    ticketRecord: TAccountMetas[4];
    managedAsset: TAccountMetas[5];
    assetAuthority: TAccountMetas[6];
  };
  data: GiftTicketInstructionData;
};

export function parseGiftTicketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGiftTicketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 7,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      currentOwner: getNextAccount(),
      recipient: getNextAccount(),
      platformConfig: getNextAccount(),
      event: getNextAccount(),
      ticketRecord: getNextAccount(),
      managedAsset: getNextAccount(),
      assetAuthority: getNextAccount(),
    },
    data: getGiftTicketInstructionDataDecoder().decode(instruction.data),
  };
}
