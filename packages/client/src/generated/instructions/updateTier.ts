/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import { findPlatformConfigPda } from '../pdas';
import { CENTLALIA_TICKETING_PROGRAM_ADDRESS } from '../programs';

export const UPDATE_TIER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  22, 250, 234, 251, 201, 246, 98, 116,
]);

export function getUpdateTierDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_TIER_DISCRIMINATOR);
}

export type UpdateTierInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountOrganizer extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountTier extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrganizer extends string
        ? ReadonlySignerAccount<TAccountOrganizer> & AccountSignerMeta<TAccountOrganizer>
        : TAccountOrganizer,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountEvent extends string ? WritableAccount<TAccountEvent> : TAccountEvent,
      TAccountTier extends string ? WritableAccount<TAccountTier> : TAccountTier,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateTierInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  priceLamports: bigint;
  supply: number;
  active: boolean;
};

export type UpdateTierInstructionDataArgs = {
  name: string;
  priceLamports: number | bigint;
  supply: number;
  active: boolean;
};

export function getUpdateTierInstructionDataEncoder(): Encoder<UpdateTierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['name', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['priceLamports', getU64Encoder()],
      ['supply', getU32Encoder()],
      ['active', getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_TIER_DISCRIMINATOR }),
  );
}

export function getUpdateTierInstructionDataDecoder(): Decoder<UpdateTierInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['name', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['priceLamports', getU64Decoder()],
    ['supply', getU32Decoder()],
    ['active', getBooleanDecoder()],
  ]);
}

export function getUpdateTierInstructionDataCodec(): Codec<
  UpdateTierInstructionDataArgs,
  UpdateTierInstructionData
> {
  return combineCodec(getUpdateTierInstructionDataEncoder(), getUpdateTierInstructionDataDecoder());
}

export type UpdateTierAsyncInput<
  TAccountOrganizer extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTier extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  platformConfig?: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  tier: Address<TAccountTier>;
  name: UpdateTierInstructionDataArgs['name'];
  priceLamports: UpdateTierInstructionDataArgs['priceLamports'];
  supply: UpdateTierInstructionDataArgs['supply'];
  active: UpdateTierInstructionDataArgs['active'];
};

export async function getUpdateTierInstructionAsync<
  TAccountOrganizer extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTier extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: UpdateTierAsyncInput<
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateTierInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: false },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
    tier: { value: input.tier ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('organizer', accounts.organizer),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('tier', accounts.tier),
    ],
    data: getUpdateTierInstructionDataEncoder().encode(args as UpdateTierInstructionDataArgs),
    programAddress,
  } as UpdateTierInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier
  >);
}

export type UpdateTierInput<
  TAccountOrganizer extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTier extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  platformConfig: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  tier: Address<TAccountTier>;
  name: UpdateTierInstructionDataArgs['name'];
  priceLamports: UpdateTierInstructionDataArgs['priceLamports'];
  supply: UpdateTierInstructionDataArgs['supply'];
  active: UpdateTierInstructionDataArgs['active'];
};

export function getUpdateTierInstruction<
  TAccountOrganizer extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTier extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: UpdateTierInput<TAccountOrganizer, TAccountPlatformConfig, TAccountEvent, TAccountTier>,
  config?: { programAddress?: TProgramAddress },
): UpdateTierInstruction<
  TProgramAddress,
  TAccountOrganizer,
  TAccountPlatformConfig,
  TAccountEvent,
  TAccountTier
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: false },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
    tier: { value: input.tier ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('organizer', accounts.organizer),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('tier', accounts.tier),
    ],
    data: getUpdateTierInstructionDataEncoder().encode(args as UpdateTierInstructionDataArgs),
    programAddress,
  } as UpdateTierInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTier
  >);
}

export type ParsedUpdateTierInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    organizer: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    event: TAccountMetas[2];
    tier: TAccountMetas[3];
  };
  data: UpdateTierInstructionData;
};

export function parseUpdateTierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateTierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      organizer: getNextAccount(),
      platformConfig: getNextAccount(),
      event: getNextAccount(),
      tier: getNextAccount(),
    },
    data: getUpdateTierInstructionDataDecoder().decode(instruction.data),
  };
}
