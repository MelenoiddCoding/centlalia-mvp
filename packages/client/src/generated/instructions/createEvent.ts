/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import { findEventPda, findPlatformConfigPda } from '../pdas';
import { CENTLALIA_TICKETING_PROGRAM_ADDRESS } from '../programs';
import {
  getEventDetailsDecoder,
  getEventDetailsEncoder,
  type EventDetails,
  type EventDetailsArgs,
} from '../types';

export const CREATE_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  49, 219, 29, 203, 22, 98, 100, 87,
]);

export function getCreateEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_EVENT_DISCRIMINATOR);
}

export type CreateEventInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountOrganizer extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrganizer extends string
        ? WritableSignerAccount<TAccountOrganizer> & AccountSignerMeta<TAccountOrganizer>
        : TAccountOrganizer,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountEvent extends string ? WritableAccount<TAccountEvent> : TAccountEvent,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateEventInstructionData = {
  discriminator: ReadonlyUint8Array;
  eventId: bigint;
  details: EventDetails;
};

export type CreateEventInstructionDataArgs = {
  eventId: number | bigint;
  details: EventDetailsArgs;
};

export function getCreateEventInstructionDataEncoder(): Encoder<CreateEventInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['eventId', getU64Encoder()],
      ['details', getEventDetailsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_EVENT_DISCRIMINATOR }),
  );
}

export function getCreateEventInstructionDataDecoder(): Decoder<CreateEventInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['eventId', getU64Decoder()],
    ['details', getEventDetailsDecoder()],
  ]);
}

export function getCreateEventInstructionDataCodec(): Codec<
  CreateEventInstructionDataArgs,
  CreateEventInstructionData
> {
  return combineCodec(
    getCreateEventInstructionDataEncoder(),
    getCreateEventInstructionDataDecoder(),
  );
}

export type CreateEventAsyncInput<
  TAccountOrganizer extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  platformConfig?: Address<TAccountPlatformConfig>;
  event?: Address<TAccountEvent>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventId: CreateEventInstructionDataArgs['eventId'];
  details: CreateEventInstructionDataArgs['details'];
};

export async function getCreateEventInstructionAsync<
  TAccountOrganizer extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: CreateEventAsyncInput<
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateEventInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }
  if (!accounts.event.value) {
    accounts.event.value = await findEventPda({
      organizer: getAddressFromResolvedInstructionAccount('organizer', accounts.organizer.value),
      eventId: getNonNullResolvedInstructionInput('eventId', args.eventId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('organizer', accounts.organizer),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getCreateEventInstructionDataEncoder().encode(args as CreateEventInstructionDataArgs),
    programAddress,
  } as CreateEventInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountSystemProgram
  >);
}

export type CreateEventInput<
  TAccountOrganizer extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  platformConfig: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventId: CreateEventInstructionDataArgs['eventId'];
  details: CreateEventInstructionDataArgs['details'];
};

export function getCreateEventInstruction<
  TAccountOrganizer extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: CreateEventInput<
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateEventInstruction<
  TProgramAddress,
  TAccountOrganizer,
  TAccountPlatformConfig,
  TAccountEvent,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('organizer', accounts.organizer),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getCreateEventInstructionDataEncoder().encode(args as CreateEventInstructionDataArgs),
    programAddress,
  } as CreateEventInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountSystemProgram
  >);
}

export type ParsedCreateEventInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    organizer: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    event: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateEventInstructionData;
};

export function parseCreateEventInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateEventInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 4,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      organizer: getNextAccount(),
      platformConfig: getNextAccount(),
      event: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateEventInstructionDataDecoder().decode(instruction.data),
  };
}
