/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import {
  findAssetAuthorityPda,
  findCheckInIntentPda,
  findManagedAssetPda,
  findPlatformConfigPda,
} from '../pdas';
import { CENTLALIA_TICKETING_PROGRAM_ADDRESS } from '../programs';

export const PRESENT_CHECK_IN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  26, 31, 84, 165, 124, 127, 126, 218,
]);

export function getPresentCheckInDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PRESENT_CHECK_IN_DISCRIMINATOR);
}

export type PresentCheckInInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountHolder extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountTicketRecord extends string | AccountMeta<string> = string,
  TAccountManagedAsset extends string | AccountMeta<string> = string,
  TAccountAssetAuthority extends string | AccountMeta<string> = string,
  TAccountCheckInIntent extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHolder extends string
        ? WritableSignerAccount<TAccountHolder> & AccountSignerMeta<TAccountHolder>
        : TAccountHolder,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountEvent extends string ? ReadonlyAccount<TAccountEvent> : TAccountEvent,
      TAccountTicketRecord extends string
        ? WritableAccount<TAccountTicketRecord>
        : TAccountTicketRecord,
      TAccountManagedAsset extends string
        ? ReadonlyAccount<TAccountManagedAsset>
        : TAccountManagedAsset,
      TAccountAssetAuthority extends string
        ? ReadonlyAccount<TAccountAssetAuthority>
        : TAccountAssetAuthority,
      TAccountCheckInIntent extends string
        ? WritableAccount<TAccountCheckInIntent>
        : TAccountCheckInIntent,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PresentCheckInInstructionData = {
  discriminator: ReadonlyUint8Array;
  intentNonce: bigint;
  expiresAt: bigint;
};

export type PresentCheckInInstructionDataArgs = {
  intentNonce: number | bigint;
  expiresAt: number | bigint;
};

export function getPresentCheckInInstructionDataEncoder(): FixedSizeEncoder<PresentCheckInInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['intentNonce', getU64Encoder()],
      ['expiresAt', getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PRESENT_CHECK_IN_DISCRIMINATOR }),
  );
}

export function getPresentCheckInInstructionDataDecoder(): FixedSizeDecoder<PresentCheckInInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['intentNonce', getU64Decoder()],
    ['expiresAt', getI64Decoder()],
  ]);
}

export function getPresentCheckInInstructionDataCodec(): FixedSizeCodec<
  PresentCheckInInstructionDataArgs,
  PresentCheckInInstructionData
> {
  return combineCodec(
    getPresentCheckInInstructionDataEncoder(),
    getPresentCheckInInstructionDataDecoder(),
  );
}

export type PresentCheckInAsyncInput<
  TAccountHolder extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTicketRecord extends string = string,
  TAccountManagedAsset extends string = string,
  TAccountAssetAuthority extends string = string,
  TAccountCheckInIntent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  holder: TransactionSigner<TAccountHolder>;
  platformConfig?: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  ticketRecord: Address<TAccountTicketRecord>;
  managedAsset?: Address<TAccountManagedAsset>;
  assetAuthority?: Address<TAccountAssetAuthority>;
  checkInIntent?: Address<TAccountCheckInIntent>;
  systemProgram?: Address<TAccountSystemProgram>;
  intentNonce: PresentCheckInInstructionDataArgs['intentNonce'];
  expiresAt: PresentCheckInInstructionDataArgs['expiresAt'];
};

export async function getPresentCheckInInstructionAsync<
  TAccountHolder extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTicketRecord extends string,
  TAccountManagedAsset extends string,
  TAccountAssetAuthority extends string,
  TAccountCheckInIntent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: PresentCheckInAsyncInput<
    TAccountHolder,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountCheckInIntent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PresentCheckInInstruction<
    TProgramAddress,
    TAccountHolder,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountCheckInIntent,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    holder: { value: input.holder ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: false },
    ticketRecord: { value: input.ticketRecord ?? null, isWritable: true },
    managedAsset: { value: input.managedAsset ?? null, isWritable: false },
    assetAuthority: { value: input.assetAuthority ?? null, isWritable: false },
    checkInIntent: { value: input.checkInIntent ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }
  if (!accounts.managedAsset.value) {
    accounts.managedAsset.value = await findManagedAssetPda({
      ticketRecord: getAddressFromResolvedInstructionAccount(
        'ticketRecord',
        accounts.ticketRecord.value,
      ),
    });
  }
  if (!accounts.assetAuthority.value) {
    accounts.assetAuthority.value = await findAssetAuthorityPda({
      platformConfig: getAddressFromResolvedInstructionAccount(
        'platformConfig',
        accounts.platformConfig.value,
      ),
    });
  }
  if (!accounts.checkInIntent.value) {
    accounts.checkInIntent.value = await findCheckInIntentPda({
      ticketRecord: getAddressFromResolvedInstructionAccount(
        'ticketRecord',
        accounts.ticketRecord.value,
      ),
      intentNonce: getNonNullResolvedInstructionInput('intentNonce', args.intentNonce),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('holder', accounts.holder),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('ticketRecord', accounts.ticketRecord),
      getAccountMeta('managedAsset', accounts.managedAsset),
      getAccountMeta('assetAuthority', accounts.assetAuthority),
      getAccountMeta('checkInIntent', accounts.checkInIntent),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getPresentCheckInInstructionDataEncoder().encode(
      args as PresentCheckInInstructionDataArgs,
    ),
    programAddress,
  } as PresentCheckInInstruction<
    TProgramAddress,
    TAccountHolder,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountCheckInIntent,
    TAccountSystemProgram
  >);
}

export type PresentCheckInInput<
  TAccountHolder extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
  TAccountTicketRecord extends string = string,
  TAccountManagedAsset extends string = string,
  TAccountAssetAuthority extends string = string,
  TAccountCheckInIntent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  holder: TransactionSigner<TAccountHolder>;
  platformConfig: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
  ticketRecord: Address<TAccountTicketRecord>;
  managedAsset: Address<TAccountManagedAsset>;
  assetAuthority: Address<TAccountAssetAuthority>;
  checkInIntent: Address<TAccountCheckInIntent>;
  systemProgram?: Address<TAccountSystemProgram>;
  intentNonce: PresentCheckInInstructionDataArgs['intentNonce'];
  expiresAt: PresentCheckInInstructionDataArgs['expiresAt'];
};

export function getPresentCheckInInstruction<
  TAccountHolder extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TAccountTicketRecord extends string,
  TAccountManagedAsset extends string,
  TAccountAssetAuthority extends string,
  TAccountCheckInIntent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: PresentCheckInInput<
    TAccountHolder,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountCheckInIntent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PresentCheckInInstruction<
  TProgramAddress,
  TAccountHolder,
  TAccountPlatformConfig,
  TAccountEvent,
  TAccountTicketRecord,
  TAccountManagedAsset,
  TAccountAssetAuthority,
  TAccountCheckInIntent,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    holder: { value: input.holder ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: false },
    ticketRecord: { value: input.ticketRecord ?? null, isWritable: true },
    managedAsset: { value: input.managedAsset ?? null, isWritable: false },
    assetAuthority: { value: input.assetAuthority ?? null, isWritable: false },
    checkInIntent: { value: input.checkInIntent ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('holder', accounts.holder),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
      getAccountMeta('ticketRecord', accounts.ticketRecord),
      getAccountMeta('managedAsset', accounts.managedAsset),
      getAccountMeta('assetAuthority', accounts.assetAuthority),
      getAccountMeta('checkInIntent', accounts.checkInIntent),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getPresentCheckInInstructionDataEncoder().encode(
      args as PresentCheckInInstructionDataArgs,
    ),
    programAddress,
  } as PresentCheckInInstruction<
    TProgramAddress,
    TAccountHolder,
    TAccountPlatformConfig,
    TAccountEvent,
    TAccountTicketRecord,
    TAccountManagedAsset,
    TAccountAssetAuthority,
    TAccountCheckInIntent,
    TAccountSystemProgram
  >);
}

export type ParsedPresentCheckInInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    holder: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    event: TAccountMetas[2];
    ticketRecord: TAccountMetas[3];
    managedAsset: TAccountMetas[4];
    assetAuthority: TAccountMetas[5];
    checkInIntent: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: PresentCheckInInstructionData;
};

export function parsePresentCheckInInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPresentCheckInInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 8,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      holder: getNextAccount(),
      platformConfig: getNextAccount(),
      event: getNextAccount(),
      ticketRecord: getNextAccount(),
      managedAsset: getNextAccount(),
      assetAuthority: getNextAccount(),
      checkInIntent: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPresentCheckInInstructionDataDecoder().decode(instruction.data),
  };
}
