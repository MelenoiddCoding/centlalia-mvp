/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import { findPlatformConfigPda } from '../pdas';
import { CENTLALIA_TICKETING_PROGRAM_ADDRESS } from '../programs';

export const PUBLISH_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  66, 121, 175, 133, 20, 2, 221, 42,
]);

export function getPublishEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PUBLISH_EVENT_DISCRIMINATOR);
}

export type PublishEventInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountOrganizer extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrganizer extends string
        ? ReadonlySignerAccount<TAccountOrganizer> & AccountSignerMeta<TAccountOrganizer>
        : TAccountOrganizer,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountEvent extends string ? WritableAccount<TAccountEvent> : TAccountEvent,
      ...TRemainingAccounts,
    ]
  >;

export type PublishEventInstructionData = { discriminator: ReadonlyUint8Array };

export type PublishEventInstructionDataArgs = {};

export function getPublishEventInstructionDataEncoder(): FixedSizeEncoder<PublishEventInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PUBLISH_EVENT_DISCRIMINATOR }),
  );
}

export function getPublishEventInstructionDataDecoder(): FixedSizeDecoder<PublishEventInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getPublishEventInstructionDataCodec(): FixedSizeCodec<
  PublishEventInstructionDataArgs,
  PublishEventInstructionData
> {
  return combineCodec(
    getPublishEventInstructionDataEncoder(),
    getPublishEventInstructionDataDecoder(),
  );
}

export type PublishEventAsyncInput<
  TAccountOrganizer extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  platformConfig?: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
};

export async function getPublishEventInstructionAsync<
  TAccountOrganizer extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: PublishEventAsyncInput<TAccountOrganizer, TAccountPlatformConfig, TAccountEvent>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublishEventInstruction<TProgramAddress, TAccountOrganizer, TAccountPlatformConfig, TAccountEvent>
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: false },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.platformConfig.value) {
    accounts.platformConfig.value = await findPlatformConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('organizer', accounts.organizer),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
    ],
    data: getPublishEventInstructionDataEncoder().encode({}),
    programAddress,
  } as PublishEventInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent
  >);
}

export type PublishEventInput<
  TAccountOrganizer extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountEvent extends string = string,
> = {
  organizer: TransactionSigner<TAccountOrganizer>;
  platformConfig: Address<TAccountPlatformConfig>;
  event: Address<TAccountEvent>;
};

export function getPublishEventInstruction<
  TAccountOrganizer extends string,
  TAccountPlatformConfig extends string,
  TAccountEvent extends string,
  TProgramAddress extends Address = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
>(
  input: PublishEventInput<TAccountOrganizer, TAccountPlatformConfig, TAccountEvent>,
  config?: { programAddress?: TProgramAddress },
): PublishEventInstruction<
  TProgramAddress,
  TAccountOrganizer,
  TAccountPlatformConfig,
  TAccountEvent
> {
  // Program address.
  const programAddress = config?.programAddress ?? CENTLALIA_TICKETING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organizer: { value: input.organizer ?? null, isWritable: false },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('organizer', accounts.organizer),
      getAccountMeta('platformConfig', accounts.platformConfig),
      getAccountMeta('event', accounts.event),
    ],
    data: getPublishEventInstructionDataEncoder().encode({}),
    programAddress,
  } as PublishEventInstruction<
    TProgramAddress,
    TAccountOrganizer,
    TAccountPlatformConfig,
    TAccountEvent
  >);
}

export type ParsedPublishEventInstruction<
  TProgram extends string = typeof CENTLALIA_TICKETING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    organizer: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    event: TAccountMetas[2];
  };
  data: PublishEventInstructionData;
};

export function parsePublishEventInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublishEventInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      organizer: getNextAccount(),
      platformConfig: getNextAccount(),
      event: getNextAccount(),
    },
    data: getPublishEventInstructionDataDecoder().decode(instruction.data),
  };
}
