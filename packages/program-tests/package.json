{
  "name": "@centlalia/program-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:program": "cargo test -p centlalia-ticketing --lib",
    "check:program": "cargo check -p centlalia-ticketing --all-targets",
    "test:integration": "vitest run --config vitest.config.ts",
    "check:integration": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@centlalia/client": "workspace:*",
    "@solana/kit": "7.0.0"
  },
  "devDependencies": {
    "@types/node": "22.16.3",
    "typescript": "5.8.3",
    "vitest": "3.2.4"
  }
}
